@inherits UmbracoViewPage<INavigation>
<!-- Nav -->
<nav id="nav" class="skel-panels-fixed">
    <ul>
        @foreach (var item in Model.MenuItems)
        {
            <li class="@(item.IsActive ? "current_page_item" : null)"><a href="@item.Url" target="@item.Target">@item.Name</a></li>
        }
    </ul>
</nav>
<!-- /Nav -->